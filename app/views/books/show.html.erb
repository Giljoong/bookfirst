<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @book.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @book.content %>
</p>

<p>
  <strong>Avatar:</strong>
  <a href="<%= @book.avatar.url %>">파일다운로드</a>
</p>

<p>
<img src="<%= @book.image.url %>"/>
</p>

<%= link_to 'Edit', edit_book_path(@book) %> |
<%= link_to 'Back', books_path %>
